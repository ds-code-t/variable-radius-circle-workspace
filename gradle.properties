org.gradle.jvmargs=-Xmx1g
# The OpenJFX Gradle plugin currently performs runtime setup for the JavaExec
# run task that is not compatible with Gradle's configuration cache.
# Keep this disabled for reliable IDE and command-line launches.
org.gradle.configuration-cache=false
