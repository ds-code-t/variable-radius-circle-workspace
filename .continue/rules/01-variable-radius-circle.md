# Variable Radius Circle Workspace

Read repository-root `AGENTS.md` first for every code or review task. It is the canonical contract. Automatically load the linked `.ai/` context and `docs/agent/feature-map.md`, preserve its domain invariants, and update affected tests and living documents.
