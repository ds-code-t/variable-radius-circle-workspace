# Variable Radius Circle Workspace Guidelines

Read and obey repository-root `AGENTS.md` before analysis or edits. Automatically load the canonical `.ai/` files and feature map it names.

For functionality changes, implement code, focused verification, affected living-document updates, index refreshes, and validation as one coherent task.
