# Variable Radius Circle Workspace

Before any repository task, read and follow `AGENTS.md`. Load all mandatory `.ai/` context and the living feature map without requiring the user to mention them. Use the functionality-change workflow and completion rules in `AGENTS.md`.
