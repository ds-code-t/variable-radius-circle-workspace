# Variable Radius Circle Workspace Rule

Treat repository-root `AGENTS.md` as the canonical project instruction. Read it and the linked `.ai/` documents before editing. Update affected tests and living context in the same change, and run the validation commands it defines.
